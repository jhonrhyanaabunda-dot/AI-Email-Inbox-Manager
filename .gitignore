node_modules
.next
out
build
dist
.env
.env.local
.env*.local
*.log
.DS_Store
coverage
tsconfig.tsbuildinfo
.turbo
.vercel
.idea
.vscode
# SQLite journal (transient)
prisma/*.db-journal
prisma/migrations/dev.db*
# Note: prisma/dev.db IS committed — it ships pre-seeded so the Vercel demo
# works without any database setup. Each serverless cold-start copies it from
# the read-only bundle to /tmp where SQLite can write.
